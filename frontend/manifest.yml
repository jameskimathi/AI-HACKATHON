---
applications:
    - name: DocMorris_Frontend
      random-route: true
      path: .
      memory: 1024M
      buildpacks:
          - python_buildpack
      env:
          STREAMLIT_SERVER_MAX_UPLOAD_SIZE: 100
